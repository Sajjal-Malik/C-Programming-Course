#include <stdio.h>

struct employee
{
    int code;
    float salary;
    char name[10];
};

int main()
{

   //     struct employee employee[100]; // an array of structures
    //     // we can access the data using:
    //     employee[0].code = 100;
    //     employee[1].code = 101;
    // And so on

    // struct employee harry = {100, 71.22, "harry"};
    // printf("%d\n", harry.code);

    // struct employee *ptr;
    // ptr = &e1;

    return 0;
}
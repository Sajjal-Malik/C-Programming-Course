#include<stdio.h>

int main() {
     // To run C programs in an Editor we need a compiler like gcc that we install using mingw32
    printf("I am learning by Code with Harry!");

    /* return 0: A return 0 means that the 
    program will execute successfully
    and did what it was intended to do. */

    return 0;

}